import { num, toUpperCase } from 'maths'

// const { num } = require('maths')
const upperText = toUpperCase('this is a test text.')
console.log(num, upperText)

// Aise modules jo ki node js modules folder ke andar ho use nmp modules kahte hai
// Those modules which are present inside node modules folder are called npm moduels
